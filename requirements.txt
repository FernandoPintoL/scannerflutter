# Web framework and utilities
flask==3.1.1
Werkzeug==3.1.3

# Environment variables
python-dotenv>=1.0.0

# Computer vision - Only one OpenCV package should be installed
supervision==0.25.1 # Computer vision tools
opencv-python==4.10.0.84  # Commented out to avoid conflicts with opencv-python-headless
opencv-python-headless==4.10.0.84 # Versión compatible con Roboflow y Ultralytics

# Roboflow API client
roboflow==1.1.65

# WSGI HTTP server (for production)
# Note: Not needed for local development, and may not install on Windows
gunicorn==21.2.0

# OCR for text extraction
easyocr==1.7.2
torch==2.2.2 # Evita que EasyOCR installe una versión incompatible

#inference
inference==0.50.3
ultralytics==8.2.7 # Necesario para inference

# Additional dependencies required by the inference package
# Using numpy 1.26.4 for compatibility with Python 3.12.0
numpy==1.26.4
pillow==10.2.0

protobuf==3.20.3 # Required by inference package